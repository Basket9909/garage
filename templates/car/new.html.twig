{% extends 'base.html.twig' %}

{% block title %}Ajouter une annonce{% endblock %}


    {% form_theme myForm 'car/_collection.html.twig' %}

{% block body %}
<div class="container">

 <h1>Création d'une nouvelle annonce</h1>

        {{ form_start(myForm) }}
            {{form_widget(myForm)}}
            <button type="submit" class="btn btn-primary mt-3">Ajouter la voiture</button>
        {{ form_end(myForm) }}

</div>

{% endblock %}

